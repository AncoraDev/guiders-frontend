import { describe, it, expect } from 'vitest';
import { Atencion } from './atencion';

describe('Atencion', () => {
  it('exporta el componente Atención', () => {
    expect(Atencion).toBeTruthy();
  });
});
